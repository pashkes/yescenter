doctype html
html(class="no-js", lang="ru")

  include ../components/head/head.pug
  include ../components/footer/footer.pug
  include ../components/header/header
  include ../components/breadcrumb/breadcrumb
  include ../components/subscribe/subscribe
  include ../components/page-reg/page-reg
  include ../components/callback/callback
  include ../components/modal-search/modal-search
  include ../components/modal-location/modal-location
  head
    +head(head.defaults)
  body
    .page-wrapper
      +header(header)
      .container
        +breadcrumb(breadcrumb.reg)
      section.page-programs
        .container
          h1.h2.page-programs__title Регистрация на курс по иностранному языку
          p.page-programs__subtitle.subtitle Уже на протяжении 15 лет мы обучаем своих студентов иностранным языкам, благодаря чему они занимают высокооплачиваемые должности, получают престижное образование за рубежом, свободно путешествуют по миру без каких-либо языковых барьеров. Наш Центр языков предлагает своим клиентам профессиональное обучение испанскому, французскому, японскому, английскому языку и многим другим иностранным языкам.
      +page-reg()
      +subscribe()
      +footer()
    +callback()
    +modal-search()
    +modal-location()
    script(src='%=static=%js/separate-js/svg4everybody.min.js')
    script svg4everybody();

    //- Main scripts. You can replace it, but I recommend you to leave it here
    script(src='%=static=%js/main%=hash=%.js')
